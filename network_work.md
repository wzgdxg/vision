## 一、网络分层基础

1.  常用**TCP/IP 5 层模型**，只需吃透前 3 层：

    -   **1 层物理层**：网线、光纤、WiFi 电波，只传 0/1 信号，无地址无包。
    -   **2 层数据链路层**：靠**MAC 地址**通信，设备局域网内转发，支持广播 / 泛洪。
    -   **3 层网络层**：靠**IP 地址**通信，负责**跨网段、全网路由转发**。

2.  分层不是上下级管控，是**分工协作、数据逐层封装**。

## 二、二层交换机 核心要点

1.  工作在**纯二层**，**只认 MAC，不认 IP**。
2.  **交换机网口没有独立 MAC**，转发数据**不修改任何二层源 / 目标 MAC**，原样透传。
3.  MAC 地址表学习机制：**只看收到数据包的「源 MAC」**，自动记录端口与 MAC 对应关系。
4.  MAC 表有**老化时间**：长期无流量就删除记录。
5.  未知目标 MAC 处理：直接**全网泛洪**到所有端口。
6.  泛洪时其他电脑网卡会**硬件过滤 MAC**，只收发给自己的包，默认不会偷听。
7.  普通傻瓜交换机**整机有一个 MAC**，仅用于设备管理，不参与数据转发。

## 三、路由器 核心要点

1.  工作在**三层**，认 IP、管网段、跨网段转发，隔离二层广播域。
2.  路由器**WAN 口、LAN 网关接口（三层口）都有独立 MAC**；**每个三层口都是独立局域网端点**，必须有 MAC 才能二层通信。
3.  家用路由器内部结构：

    -   4 个 LAN 口 = **内置一台二层交换机**（和普通交换机无区别，端口无 MAC、不改帧）；
    -   WAN 口是**独立三层口**，单独 MAC，对接光猫。

4.  转发特性：**每经过一次路由器，拆掉旧二层帧头，重新封装新二层头**；三层 IP 全程不变，仅二层 MAC 逐跳更换。
5.  路由表：

    -   直连路由：自动识别自己连着的内网 / WAN 网段；
    -   默认路由：家用路由器所有不认识的外网 IP，全部丢给光猫 / 运营商。

6.  网关常识：**网关默认习惯是网段末尾.1**，但协议不强制，可任意设置本网段空闲 IP。

## 四、ARP 协议 核心要点

1.  作用：**已知 IP，查询对方 MAC 地址**，没有 MAC 就封装不了二层帧，无法发数据。
2.  触发条件：主机 ARP 缓存**无对应 IP 的 MAC 记录 / 缓存老化**时，必发 ARP 广播。
3.  两种使用场景：

    -   同网段：ARP 查询**目标电脑 IP**的 MAC；
    -   跨网段：不查远端电脑，只**ARP 查询网关（路由器 LAN）MAC**。

4.  ARP 本身是**二层广播帧**，交换机会自动泛洪转发。

## 五、数据转发核心铁律

1.  **同网段通信**：目标 MAC 填对方电脑 MAC，交换机二层直接转发，不走路由器。
2.  **跨网段 / 访问外网**：主机把目标 MAC 填**路由器网关 MAC**，先发给路由器；路由器拆解后重新封装 WAN 口 MAC + 光猫 MAC，转发到外网。
3.  **二层 MAC 逐跳更换，三层 IP 从头到尾不变**。

## 六、关键误区纠正

1.  不是管辖范围越大层级越底层：**越底层越简单、只管局部；越上层越智能、管全网**。
2.  交换机≠有端口 MAC，只有路由器三层口才有独立 MAC。
3.  交换机绝不修改数据包 MAC，只有路由器会重封装二层头。
4.  交换机记不住设备 MAC 没关系，下次通信泛洪 + 设备回包，自动重新学习。

## 七、主机自动知道网关 MAC 的完整原理（超通俗版）

分两步：**先知道网关 IP → 再用 ARP 自动查出网关 MAC**，全程全自动，不用你手动配置。

### 第一步：主机先自动拿到「网关 IP」

两种方式，都是自动的：

1.  **DHCP 自动获取（家用默认）**

    路由器自带 DHCP 服务，主机一插网线 / 连 WiFi，自动向路由器申请 IP；

    路由器会**自动发给主机三样东西**：

    本机 IP + 子网掩码 + **默认网关 IP**（比如 192.168.1.1）。

2.  **手动静态填 IP**

    你自己手动填上：IP、子网掩码、**网关 IP**，主机就记住了。


* * *

### 第二步：知道网关 IP 后，主机自动查网关 MAC

靠 **ARP 协议 自动广播查询**，流程全自动：

1.  主机先查自己**本地 ARP 缓存表**，有没有「网关 IP 对应 的 MAC」；
2.  **有记录**：直接拿来用，不用发包；
3.  **没记录 / 缓存老化过期**：

    主机立刻发一个 **ARP 广播帧**（目标 MAC 是全 F 广播地址），在局域网里喊：

    > 谁是 192.168.1.1（网关 IP）？请把你的 MAC 地址告诉我！

4.  交换机把这个 ARP 广播**泛洪**到所有 LAN 口；
5.  路由器 LAN 口（网关）收到广播，发现是问自己，**立刻单播回复**：

    > 我就是网关，我的 MAC 是 XX:XX:XX...

6.  主机收到回复，**自动把网关 IP+MAC 存入 ARP 缓存**，以后直接用。

* * *

### 关键总结

1.  主机**先靠 DHCP / 手动**拿到网关 IP；
2.  再靠 **ARP 广播自动问询**，拿到网关 MAC；
3.  全程系统自动完成，不需要人为干预；
4.  ARP 缓存会老化，超时后主机会**自动重新发 ARP 问一遍**；
5.  为什么能查到？**网关和主机在同一个局域网**，ARP 广播能直达路由器。

### 一句话记住

主机先拿到网关 IP，**不懂就喊（ARP 广播）**，路由器主动报 MAC，主机记下来直接用。